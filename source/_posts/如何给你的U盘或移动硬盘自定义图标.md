---
title: 如何给你的U盘或移动硬盘自定义图标
categories:
  - 无类可分
description: 上网冲浪的时候看到的一个骚操作，记录一下
date: 2019-11-27 18:21:00
---
# 操作步骤

首先，选好自己想要自定义的图片，将该图片转为32*32的ico文件。关于怎么转换，直接上网搜就好了。

然后新建一个 `autorun.inf` 文件，并编辑里面的内容为如下

```
[autorun]
icon=icon.ico
```

emm,那个icon.ico就是你那个图片转化之后的ico文件，名字是可以修改的，只要`autorun.inf`的文件内容里也跟着修改就行了，然后将ico的图片和那个 `autorun.inf`文件放在移动硬或U盘的根目录下面就完工了。如果你是U盘，就拔掉在插入，如果是移动硬盘可能就要重启了。

# 关于可能出现的问题

当你新建好 `autorn.inf`这个文件后，将它移入U盘，或者直接在U盘下新建 `autorn.inf`这个文件，可能系统会提示你没有权限执行。这个问题也卡了我好久，对于这个问题你就要赋予U盘所有的权限，即你的用户需要是管理员权限，当你弄好权限后就可以将 `autorn.inf`这个文件放进U盘了。

不过可能系统还会告诉你你没有权限执行，这里我也是查了好久，最后我查了一下 `autorn.inf`这个文件，发现该文件的作用是允许在双击磁盘时自动运行指定的某个文件，而且近几年出现了有利用该文件来传播木马和病毒，即木马或病毒可能会因为你的U盘来侵入你的电脑。折腾了一下，也查了一下，就知道了方法。只要你将你电脑的所有安全杀毒关掉，就可以将 `autorn.inf`这个文件放进去了。


> 这是从某位博主上发现的操作，时代久远，已经找不到原文了